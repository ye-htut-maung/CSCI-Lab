#include <iostream>
#include <fstream>
using namespace std;


int main() {

    // 1
    
        // ifstream f;
        // f.open("input.txt");
        // string line;
        // getline(f, line);
        // cout << line;
    
    
    // 2

    // ofstream f;
    // f.open("output.txt");
    // f << 2 << endl;
    // f.close();

    // 3

    // cout << line.size();

    // 4

    // int main (int argc, char *argv[]) {}

    // 5
    // string line = "abcde";
    
    //     if (line.find('K') >= 0 && line.find('K') < line.size() - 1) {
    //         cout << line[line.find('K') + 1] << endl;
    //     } else {
    //         cout << line[0] << endl;
    //     }
    //     cout << line.find("K");
     //
    //     if (line.find("K") >= 0 && line.find("K") < line.size() - 1) {
    //         cout << line[line.find("K") + 1] << endl;
    //     } else {
    //         cout << line[0] << endl;
    //     }
    // 
    

    // 6
    // cout << rand() % (999 - 100 + 1) + 100;
 

    return 0;
}