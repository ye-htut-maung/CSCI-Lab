#include <iostream>
#include <fstream>
using namespace std;

int main() {

    // // 1
    // ofstream f;
    // f.open("out.txt");
    // f << 7;
    // f.close();

    // // 2
    // ifstream f;
    // f.open("in.txt");
    // string s1;
    // // f >> s1;
    // getline(f, s1);
    // f.close();
    // cout << s1 << endl;

    // 3
    // int main(int argc, char *argv[]){}

    // 4
    // cout << line[4] << endl;

    // 5
    /*
        int K_place = line.find("K");
        if (K_place >= 0 && K_place < line.size() - 1){
            cout << line[K_place + 1];
        } else cout << line[0];
    */

    // 6
    // cout << rand() % (99 - 10 + 1) + 10;



    return 0;
}